spring:
  application:
    name: TimesheetRegister
  datasource:
    url: jdbc:postgresql://localhost:5432/time_register
    username: postgres
    password: pass
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
#        dialect: org.hibernate.dialect.PostqreSQLDialect