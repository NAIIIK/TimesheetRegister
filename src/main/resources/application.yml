spring:
  application:
    name: TimesheetRegister
  datasource:
    url: jdbc:postgresql://localhost:5432/time_register
    username: postgres
    password: pass
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true